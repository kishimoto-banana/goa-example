// Code generated by goa v3.0.9, DO NOT EDIT.
//
// example-api HTTP client CLI support package
//
// Command:
// $ goa gen goa-example/design

package cli

import (
	"flag"
	"fmt"
	pingc "goa-example/gen/http/ping/client"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//    command (subcommand1|subcommand2|...)
//
func UsageCommands() string {
	return `ping return- pong
`
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` ping return- pong` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, interface{}, error) {
	var (
		pingFlags = flag.NewFlagSet("ping", flag.ContinueOnError)

		pingReturnPongFlags = flag.NewFlagSet("return- pong", flag.ExitOnError)
	)
	pingFlags.Usage = pingUsage
	pingReturnPongFlags.Usage = pingReturnPongUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "ping":
			svcf = pingFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "ping":
			switch epn {
			case "return- pong":
				epf = pingReturnPongFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     interface{}
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "ping":
			c := pingc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "return- pong":
				endpoint = c.ReturnPong()
				data = nil
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// pingUsage displays the usage of the ping command and its subcommands.
func pingUsage() {
	fmt.Fprintf(os.Stderr, `Ping/Pong service
Usage:
    %s [globalflags] ping COMMAND [flags]

COMMAND:
    return- pong: ReturnPong implements Return pong.

Additional help:
    %s ping COMMAND --help
`, os.Args[0], os.Args[0])
}
func pingReturnPongUsage() {
	fmt.Fprintf(os.Stderr, `%s [flags] ping return- pong

ReturnPong implements Return pong.

Example:
    `+os.Args[0]+` ping return- pong
`, os.Args[0])
}
